# 创建账号
def test_example(page: Page) -> None:
    page.goto("https://workspace-dev.spreadwin.cn/admin/me")
    page.get_by_label("menu drawer").get_by_text("账号", exact=True).click()
    page.get_by_role("button", name="创建账号").click()
    page.get_by_role("textbox", name="登录用户名").click()
    page.get_by_role("textbox", name="登录用户名").fill("ni'de'ye")
    page.get_by_role("textbox", name="登录用户名").press("Enter")
    page.get_by_role("textbox", name="登录用户名").fill("nideye")
    page.get_by_role("textbox", name="请输入真实姓名").click()
    page.get_by_role("textbox", name="请输入真实姓名").fill("你大爷")
    page.get_by_role("textbox", name="请输入手机号", exact=True).click()
    page.get_by_role("textbox", name="请输入手机号", exact=True).fill("12345678910")
    page.get_by_role("textbox", name="请输入邮箱地址").click()
    page.get_by_role("textbox", name="请输入邮箱地址").fill("1247@qq.com")
    page.get_by_role("textbox", name="请输入邮箱地址").press("Enter")
    page.get_by_role("textbox", name="请输入邮箱地址").fill("1247@qq.com")
    page.locator("#el-id-6521-313").click()
    page.get_by_role("option", name="小镜").click()
    page.locator("#el-id-6521-314").click()
    page.get_by_role("option", name="渠道管理员").click()
    page.get_by_text("创建用户用户名真实姓名手机号E-mail").click()
    page.get_by_text("创建用户用户名真实姓名手机号E-mail").click()
    page.get_by_text("创建用户用户名真实姓名手机号E-mail").click()
    page.get_by_role("option", name="小镜").click()
    page.get_by_role("textbox", name="请输入密码").click()
    page.get_by_role("textbox", name="请输入密码").fill("qwer@!123")
    page.get_by_role("textbox", name="请再次输入密码").click()
    page.get_by_role("textbox", name="请再次输入密码").fill("qwer@123")
    page.get_by_role("button", name="确认").click()
    page.locator(".el-icon.el-input__icon.el-input__password > svg").first.click()
    page.get_by_role("textbox", name="请输入密码").click()
    page.get_by_role("textbox", name="请输入密码").press("ArrowLeft")
    page.get_by_role("textbox", name="请输入密码").press("ArrowLeft")
    page.get_by_role("textbox", name="请输入密码").press("ArrowLeft")
    page.get_by_role("textbox", name="请输入密码").fill("qwer@123")
    page.get_by_role("button", name="确认").click()
    page.locator("#el-id-6521-313").click()
    page.get_by_role("option", name="1112").click()
    page.get_by_role("button", name="确认").click()
